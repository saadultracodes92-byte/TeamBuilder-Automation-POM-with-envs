import availabilitySelectors from '../Selectors/availabilitySelectors';

class AvailabilityPage {
  // Add methods for interacting with availability page elements
  // Example: clickAvailabilityButton() { cy.get(availabilitySelectors.availabilityButton).click(); }
}

export default new AvailabilityPage();